# vsm-prototype

Built files of the Virtual Singles Mixer prototype, published automatically from the private
project repository. Nothing here is real: names, ages, places and photos are all fake.
Open any page with `?pd=1` to leave feedback pins.
